#include<iostream>
using namespace std;
int main(){
	int n;
	cout<<" Nhap so N: ";
	cin >> n;
	if(n%2==0)
	   cout<<" so "<< n <<" la so chan";
	else
	   cout<<" so "<< n <<" la so le";
	   
return 0;
}
